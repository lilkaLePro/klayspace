
export type CompenentInfo = {
    name : string,
    icon: React.ComponentType;
  }

export type ProjectInfo = {
    title : string,
    desc : string,
    icon : React.ComponentType;
}

export type WorkInfo = {
    societeName : string,
    func : string,
    date : string,
    icon : React.ComponentType;
}

export type InspiCardInfo = {
    citation : string,
    name : string,
    func : string,
    icon : React.ComponentType;
}

export type ContactInfo = {
    link : string,
    name : string,
    image : string;
    icon : React.ComponentType;
}